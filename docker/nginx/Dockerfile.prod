# ===========================================
# Nginx Production Dockerfile (ローカル本番テスト用)
# ===========================================
# docker-compose.prod.yml で API と Next.js へのリバースプロキシとして使用

FROM nginx:1.24-alpine

ENV TZ=Asia/Tokyo

# Nginx config file
COPY ./docker/nginx/default-local-prod.conf /etc/nginx/conf.d/default.conf

WORKDIR /var/www/html

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
